platform :ios, '9.0'

target 'SladerChallenge' do
  use_frameworks!
  pod 'SDWebImage'
  pod 'ReactiveSwift'
  # TODO: update to latest Dip (6.1 doesn't work for me, need investigation)
  pod 'Dip', '6.0'

  target 'SladerChallengeTests' do
    inherit! :search_paths
  end
end